Simply it scramble text on screen within a 'textDecode' id element on the DOM.

```
<div id='textDecode'>......</div>
```

Content should be a global array called `content`:

```
   var content = ['First text to show', 'Second text to show', ....];
```
